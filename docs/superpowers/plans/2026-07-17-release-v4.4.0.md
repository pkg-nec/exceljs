# Release @pkg-nec/exceljs v4.4.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Release `@pkg-nec/exceljs@4.4.0` to public npmjs.com — same functionality as `exceljs@4.4.0`, with metadata changes, supply chain hardening, and CI modernization.

**Architecture:** Metadata-only fork. All changes are to package identity files (`package.json`, `README.md`), supply chain config (`.npmrc`, `.gitignore`, `package-lock.json`), CI workflow (`.github/workflows/tests.yml`), and one file deletion (`.github/FUNDING.yml`). No source code changes.

**Tech Stack:** Node.js, npm 11.10.0, Grunt (existing build system), GitHub Actions

## Global Constraints

- npm version: 11.10.0 (required for `min-release-age` support)
- No source code changes in `lib/`, `spec/`, `test/`
- No changes to `LICENSE`, `index.d.ts`, `README_zh.md`
- All implementation work on `release/4.4.0` branch (created from `main` at commit `ac96f9a`), merged to `main` before tagging and publishing
- `master` branch must not be modified

---

### Task 1: Package Identity — Update package.json and delete FUNDING.yml

**Files:**
- Modify: `package.json:2` (name field)
- Modify: `package.json:11-14` (repository section)
- Modify: `package.json:57` (remove install-build script)
- Delete: `.github/FUNDING.yml`

**Interfaces:**
- Consumes: nothing
- Produces: Updated `package.json` with new package name and repo URL, used by all subsequent tasks

- [ ] **Step 1: Create the release branch**

```bash
git checkout main
git checkout -b release/4.4.0
```

- [ ] **Step 2: Update package name**

In `package.json`, change line 2:

```diff
-  "name": "exceljs",
+  "name": "@pkg-nec/exceljs",
```

- [ ] **Step 3: Update repository URL**

In `package.json`, change lines 11-14:

```diff
   "repository": {
     "type": "git",
-    "url": "https://github.com/exceljs/exceljs.git"
+    "url": "https://github.com/pkg-nec/exceljs.git"
   },
```

- [ ] **Step 4: Remove install-build script**

In `package.json`, delete line 57:

```diff
-    "install-build": "npm install && grunt build",
```

- [ ] **Step 5: Delete FUNDING.yml**

```bash
git rm .github/FUNDING.yml
```

- [ ] **Step 6: Verify package.json is valid JSON**

```bash
node -e "JSON.parse(require('fs').readFileSync('package.json', 'utf8')); console.log('Valid JSON')"
```

Expected: `Valid JSON`

- [ ] **Step 7: Commit**

```bash
git add package.json
git commit -m "chore: rebrand to @pkg-nec/exceljs, remove install-build script and FUNDING.yml"
```

---

### Task 2: README.md — Update references for the fork

**Files:**
- Modify: `README.md:1-17` (title, badge, install)
- Modify: `README.md:180` (require statement)
- Modify: `README.md:2534` (browser spec link)

**Interfaces:**
- Consumes: nothing
- Produces: Updated README with fork branding

- [ ] **Step 1: Update title, badge, and add fork notice**

Replace lines 1-7 of `README.md`:

```diff
-# ExcelJS
+# @pkg-nec/exceljs
 
-[![Build Status](https://github.com/exceljs/exceljs/actions/workflows/tests.yml/badge.svg?branch=master&event=push)](https://github.com/exceljs/exceljs/actions/workflows/tests.yml)
+> **This is a maintained fork of [exceljs](https://github.com/exceljs/exceljs).** The original package has been unmaintained. This fork is published as `@pkg-nec/exceljs` with the same API and functionality.
 
-Read, manipulate and write spreadsheet data and styles to XLSX and JSON.
+[![Build Status](https://github.com/pkg-nec/exceljs/actions/workflows/tests.yml/badge.svg?branch=main&event=push)](https://github.com/pkg-nec/exceljs/actions/workflows/tests.yml)
 
-Reverse engineered from Excel spreadsheet files as a project.
+Read, manipulate and write spreadsheet data and styles to XLSX and JSON.
```

- [ ] **Step 2: Update install instructions**

Replace line 16 of `README.md`:

```diff
-npm install exceljs
+npm install @pkg-nec/exceljs
```

- [ ] **Step 3: Update require statement**

Replace line 180 of `README.md`:

```diff
-const ExcelJS = require('exceljs');
+const ExcelJS = require('@pkg-nec/exceljs');
```

- [ ] **Step 4: Update browser spec link**

Replace line 2534 of `README.md`:

```diff
-For example code using ExcelJS in the browser take a look at the <a href="https://github.com/exceljs/exceljs/tree/master/spec/browser">spec/browser</a> folder in the github repo.
+For example code using ExcelJS in the browser take a look at the <a href="https://github.com/pkg-nec/exceljs/tree/main/spec/browser">spec/browser</a> folder in the github repo.
```

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: update README.md for @pkg-nec/exceljs fork"
```

---

### Task 3: Supply Chain Hardening — .npmrc, .gitignore, and package-lock.json

**Files:**
- Modify: `.npmrc` (replace content)
- Modify: `.gitignore:56` (remove package-lock.json entry)
- Create: `package-lock.json` (generated by npm install)

**Interfaces:**
- Consumes: Updated `package.json` from Task 1
- Produces: `package-lock.json` committed to repo, `min-release-age=7` in `.npmrc`

- [ ] **Step 1: Update .npmrc**

Replace entire contents of `.npmrc`:

```diff
-package-lock=false
+min-release-age=7
```

- [ ] **Step 2: Remove package-lock.json from .gitignore**

In `.gitignore`, delete line 56:

```diff
-package-lock.json
```

- [ ] **Step 3: Generate package-lock.json**

Verify npm version first, then generate the lockfile:

```bash
npm --version
```

Expected: `11.10.0`

```bash
npm install
```

This generates `package-lock.json`. The lockfile should appear as an untracked file.

- [ ] **Step 4: Verify lockfile was generated**

```bash
ls -la package-lock.json
```

Expected: File exists with non-zero size.

```bash
head -5 package-lock.json
```

Expected: JSON with `"name": "@pkg-nec/exceljs"` and `"lockfileVersion": 3`.

- [ ] **Step 5: Commit**

```bash
git add .npmrc .gitignore package-lock.json
git commit -m "chore: enable package-lock, add min-release-age=7 for supply chain hardening"
```

---

### Task 4: CI Modernization — Update GitHub Actions workflow

**Files:**
- Modify: `.github/workflows/tests.yml` (full rewrite of workflow)

**Interfaces:**
- Consumes: `package-lock.json` from Task 3 (for cache key)
- Produces: Modernized CI workflow triggering on `main` branch

- [ ] **Step 1: Rewrite tests.yml**

Replace the entire contents of `.github/workflows/tests.yml` with:

```yaml
name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ '**' ]

jobs:
  test:
    timeout-minutes: 10
    name: Node v${{ matrix.node-version }} on ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        node-version: [18.x, 20.x, 22.x, 24.x]
        os: [ubuntu-latest, macOS-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Set git config
        shell: bash
        run: |
          git config --global core.autocrlf false
          git config --global core.symlinks true
        if: runner.os == 'Windows'
      - uses: actions/checkout@v7
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
      - name: Pin npm version
        run: npm install -g npm@11.10.0
      - name: Cache node modules
        uses: actions/cache@v6
        with:
          path: ~/.npm
          key: v2-${{ runner.os }}-node-${{ matrix.node-version }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            v2-${{ runner.os }}-node-${{ matrix.node-version }}-npm-
      - run: npm ci
      - run: npm test
        env:
          CI: true

  benchmark:
    timeout-minutes: 15
    name: Measure performance impact of changes
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Pin npm version
        run: npm install -g npm@11.10.0
      - name: Cache node modules
        uses: actions/cache@v6
        with:
          path: ~/.npm
          key: v2-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            v2-npm-
      - run: npm ci
      - run: npm run benchmark
        env:
          CI: true

  typescript:
    timeout-minutes: 15
    name: Ensure typescript compatibility
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Pin npm version
        run: npm install -g npm@11.10.0
      - name: Cache node modules
        uses: actions/cache@v6
        with:
          path: ~/.npm
          key: v2-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            v2-npm-
      - run: npm ci
      - run: tsc index.d.ts
        env:
          CI: true
```

Key changes from the original:
- Branch trigger: `master` → `main`
- `npm install` → `npm ci` (3 occurrences)
- `actions/checkout`: v2 (pinned SHA) → `v7`
- `actions/setup-node`: v2 (pinned SHA) → `v7`
- `actions/cache`: v2 (pinned SHA) → `v6`
- Node matrix: `[10.x, 12.x, 14.x, 16.x, 17.x, 18.x, 19.x, 20.x]` → `[18.x, 20.x, 22.x, 24.x]`
- Added `npm install -g npm@11.10.0` step in all jobs
- Cache path: custom `./npm-cache` → default `~/.npm`
- Cache key: `hashFiles('**/package.json')` → `hashFiles('**/package-lock.json')`
- Cache key prefix: `v1-` → `v2-` (bust old caches)
- Removed manual `mkdir npm-cache && npm config set cache` step
- Removed separate `npm install typescript` step (typescript is in devDependencies)

- [ ] **Step 2: Validate YAML syntax**

```bash
node -e "const yaml = require('js-yaml'); const fs = require('fs'); yaml.load(fs.readFileSync('.github/workflows/tests.yml', 'utf8')); console.log('Valid YAML')" 2>/dev/null || python3 -c "import yaml; yaml.safe_load(open('.github/workflows/tests.yml')); print('Valid YAML')"
```

Expected: `Valid YAML`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/tests.yml
git commit -m "ci: modernize workflow - update actions, node matrix, use npm ci with pinned npm@11.10.0"
```

---

### Task 5: Build, Test, Merge, Tag & Publish

**Files:**
- No file modifications — this task runs build/test/merge/publish commands

**Interfaces:**
- Consumes: All changes from Tasks 1–4 on `release/4.4.0` branch
- Produces: Merged `main` branch, tagged release on GitHub, published package on npmjs.com

- [ ] **Step 1: Build the browser bundle**

```bash
npx grunt build
```

Expected: Grunt completes successfully, `dist/` directory is populated with `exceljs.js`, `exceljs.min.js`, and `es5/` subdirectory.

```bash
ls dist/
```

Expected: Files present in `dist/`.

- [ ] **Step 2: Run the test suite**

```bash
npm test
```

Expected: All tests pass. This runs `npm run build && npm run test:unit && npm run test:integration && npm run test:end-to-end && npm run test:jasmine`.

- [ ] **Step 3: Dry-run the publish**

```bash
npm publish --access public --dry-run
```

Expected: Shows the package tarball contents including `dist/`, `lib/`, `excel.js`, `LICENSE`, `README.md`, `index.ts`, `index.d.ts`. Package name should be `@pkg-nec/exceljs`. No errors.

- [ ] **Step 4: Merge release branch into main**

```bash
git checkout main
git merge --no-ff release/4.4.0 -m "release: merge release/4.4.0 into main for @pkg-nec/exceljs v4.4.0"
```

- [ ] **Step 5: Tag the release on main**

```bash
git tag v4.4.0
```

- [ ] **Step 6: Push to GitHub**

```bash
git push origin main --tags
```

- [ ] **Step 7: Publish to npmjs.com**

```bash
npm publish --access public
```

Expected: Package published successfully as `@pkg-nec/exceljs@4.4.0`.

- [ ] **Step 8: Verify the published package**

```bash
npm view @pkg-nec/exceljs
```

Expected: Shows package metadata with version `4.4.0`, correct description, and repository URL pointing to `https://github.com/pkg-nec/exceljs.git`.

- [ ] **Step 9: Clean up release branch**

```bash
git branch -d release/4.4.0
```
